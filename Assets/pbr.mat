
# The "#" symbol is used for comments (which are ignored by the engine)
# This is pretty much the structure:

:/material
# The first one is a keyword, the second one is the asset path
vertex_shader: CompiledShaders/model2proj.shader
pixel_shader:  CompiledShaders/pbr.shader

:/textures

:/uniforms
