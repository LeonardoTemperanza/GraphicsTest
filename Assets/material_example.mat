
# The '#' symbol is used for comments (which are ignored by the engine)
# This is pretty much the structure:

:/material
# The first one is a keyword, the second one is the asset string id / asset purpose
vertex_shader: model2projection
pixel_shader:  simple_pixel

:/textures
0: 

:/uniforms
roughness: 2.0
metalness: 1.5
is_bird: true
