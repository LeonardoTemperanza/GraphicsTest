Materials will contain references to shaders, textures, etc.

When importing models, the shader will just be the standard PBR shader with its associated textures (or any predefined shader which can just be changed later in an editor or something)
